# 我把网站搬到Cloudflare了，一年省下600块，还免费送CDN加速！

> 一个量化交易博主的"薅羊毛"实录，顺便安利一下我的新站点

## 前言

大家好，我是**王者quant**。

熟悉我的朋友都知道，我一直在维护一个QMT/PTrade量化交易技术文档网站——**ptqmt.com**。这个网站从上线到现在，帮助了不少朋友从零开始入门量化交易。

但有个问题一直困扰着我：**服务器太贵了**。

阿里云2核2G的轻量服务器，一年要 **600块**。对于一个访问量不大的个人文档站来说，这笔钱花得实在心疼。

最近我终于下定决心，把整个网站迁移到了 **Cloudflare Pages**——完全免费。今天就来和大家聊聊这次迁移的经历，顺便正式介绍一下我的新站点地址。

---

## 🌐 新站点地址

先说重点，网站现在可以通过两个地址访问：

- 🆕 **新地址**：[https://ptqmt-site.pages.dev](https://ptqmt-site.pages.dev)
- 🏠 **原地址**：[https://ptqmt.com](https://ptqmt.com)（9月份服务器到期后也会迁移到Cloudflare）

内容和以前一模一样，只是换了个更省钱的方式部署。大家可以把新地址收藏起来，以后随时查阅。

---

## 💰 为什么要迁移？

先给大家算一笔账：

| 项目 | 之前（阿里云） | 现在（Cloudflare） |
|------|:------:|:------:|
| 服务器费用 | 600元/年 | **0元** |
| SSL证书 | 已包含 | **0元（自动配置）** |
| CDN加速 | 无 | **免费送** |
| DDoS防护 | 无 | **免费送** |
| 每月流量 | 有限制 | **100GB免费** |
| 每日请求 | 有限制 | **10万次免费** |

一年省下600块，对于一个个人网站来说，这钱拿去买杯咖啡、充个视频会员不香吗？

---

## 🤔 Cloudflare 是什么？

可能有些朋友对Cloudflare不太熟悉，简单介绍一下：

**Cloudflare** 是全球最大的网络安全和性能优化公司之一，成立于2009年，总部在美国旧金山。全球超过 **20%** 的网站都在使用Cloudflare的服务。

你可能会问：这么牛的公司，为什么会免费？

因为Cloudflare的商业模式很简单：**免费版吸引用户，付费版赚企业的钱**。对于个人用户和小型网站，他们的免费套餐已经绰绰有余了。这也是为什么很多独立开发者、技术博主都选择Cloudflare来托管自己的网站。

### Cloudflare 免费套餐能干什么？

- ✅ **Pages 静态网站托管** —— 无限站点，免费部署
- ✅ **全球CDN加速** —— 200多个国家/地区有节点
- ✅ **自动SSL证书** —— HTTPS加密，自动续期
- ✅ **DDoS防护** —— 抵御恶意攻击
- ✅ **自定义域名绑定** —— 支持绑定你自己的域名
- ✅ **自动部署** —— 代码推送到GitHub，网站自动更新

---

## 🔧 迁移过程有多简单？

说实话，整个过程不到 **30分钟** 就搞定了。大致步骤如下：

### 第一步：把网站代码推到GitHub

我的网站是用 **Docsify** 搭建的纯静态文档站（HTML + CSS + JS），没有任何后端逻辑，非常适合这种免费托管方案。

```bash
# 初始化仓库
git init
git add .
git commit -m "init: ptqmt.com docsify site"

# 推送到GitHub
git remote add origin https://github.com/quant-king299/ptqmt-site.git
git push -u origin main
```

### 第二步：Cloudflare Pages 一键部署

1. 注册 Cloudflare 账号（免费）
2. 进入 Workers & Pages → 创建项目 → 连接 GitHub 仓库
3. 选择 `ptqmt-site` 仓库
4. 构建设置全部留空（纯静态站不需要构建）
5. 点击部署 ✅

### 第三步：绑定自定义域名（可选）

如果你有自己的域名（比如我的 ptqmt.com），可以在 Cloudflare 里绑定，只需要把域名的 DNS 服务器改成 Cloudflare 提供的就行。我打算等9月份阿里云服务器到期后再做这一步。

### 以后更新网站

只需要三行命令：

```bash
git add .
git commit -m "更新内容"
git push
```

Cloudflare 会自动检测到代码变更，1-2分钟内完成重新部署。比之前登录服务器、手动传文件方便太多了。

---

## 🎯 什么样的网站适合迁移到 Cloudflare？

如果你也有个人网站，可以对照看看是否适合：

### ✅ 非常适合
- **静态文档站**（Docsify、VuePress、Hexo、Hugo 等）
- **个人博客**
- **作品集/简历网站**
- **开源项目文档**
- **着陆页/宣传页**

### ❌ 不适合
- 需要后端服务器运行的动态网站（如 WordPress、Django）
- 需要数据库的网站
- 有用户登录、支付等交互功能的网站

简单来说：**只要你的网站是纯静态的（HTML/CSS/JS），就可以免费部署到 Cloudflare Pages。**

---

## 📚 网站里有什么内容？

顺便给大家介绍一下我的网站上都有什么干货：

### 📊 QMT 量化交易平台专区
- 软件安装与配置指南
- 模型研究与回测系统
- QMT API 完整参考手册
- 高级策略开发技术
- 常见问题解答（FAQ）

### 📱 MiniQMT / EasyXT 专区
- MiniQMT 快速入门
- EasyXT 扩展交易接口教程
- 数据获取与处理技巧

### 📈 PTrade 交易平台专区
- PTrade 平台快速入门
- 环境配置详细指南
- PTrade API 完整参考
- 策略开发实例教程

### 📖 通达信量化交易 API 文档
- 市场数据获取
- 财务数据获取
- 实时行情数据
- 交易下单接口
- 板块数据获取

### 🎓 学习案例专区
- 基础入门教程
- 交易基础教程
- 高级交易教程
- 策略回测教程
- 实盘策略开发教程
- QStock 真实交易案例

无论你是量化交易初学者，还是有一定经验的策略开发者，都能在网站上找到有用的内容。

---

## 📌 写在最后

这次迁移给我的最大感触是：**2025年了，个人建站的成本真的可以降到几乎为零。**

域名续费一年也就几十块，再加上 Cloudflare 的免费托管，一个网站一年的维护成本不超过一杯星巴克。这在十年前是不可想象的。

如果你也有自己的个人网站，而且在为服务器费用发愁，强烈建议试试 Cloudflare Pages。如果你在建站过程中遇到问题，欢迎留言交流，我看到会回复的。

最后，**欢迎大家收藏我的新站点** 👇

### 🔗 [https://ptqmt-site.pages.dev](https://ptqmt-site.pages.dev)

以后所有QMT/PTrade的教程和文档更新，都会第一时间在网站上同步。有什么想看的内容，也欢迎在评论区告诉我！

---

**关于我**：王者quant，专注量化交易技术分享，B站/微信公众号同名。需要开通QMT/PTrade可以加微信：**www_ptqmt_com**

如果觉得有用，别忘了 **点赞 + 在看 + 收藏** 三连支持一下 ❤️
